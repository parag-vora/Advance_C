#include"maths.h"

int multiplyFunction(const int tempValue, const int factor){
    return tempValue * factor;
}

float divideFunction(const int tempValue, const int factor){
    return tempValue / factor;
}